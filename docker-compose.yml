services:
  pgvector:
    image: pgvector/pgvector:pg16
    container_name: forge-pgvector
    environment:
      POSTGRES_DB: forge_kb
      POSTGRES_USER: forge
      POSTGRES_PASSWORD: ForgeKB2025!
    ports:
      - "5434:5432"
    volumes:
      - pgvector_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  pgvector_data:
